# Database MySQL (per il container Docker)
MYSQL_ROOT_PASSWORD=rootpassword123
MYSQL_DATABASE=sport4u
MYSQL_USER=dbuser
MYSQL_PASSWORD=dbpassword123

# Porta del database (opzionale, default: 3307)
DB_PORT=3307

# Configurazione database per l'applicazione
DB_HOST=db
DB_USER=dbuser
DB_PASSWORD=dbpassword123
DB_NAME=sport4u

# Porta dell'applicazione (opzionale, default: 3000)
PORT=3000

# Secret per JWT (usa una stringa casuale sicura in produzione)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
